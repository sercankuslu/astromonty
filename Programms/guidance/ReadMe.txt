========================================================================
ПРИЛОЖЕНИЕ WIN32. Общие сведения guidance
========================================================================

Это приложение guidance создано автоматически с помощью 
мастера приложений.

   В этом файле представлена сводка содержимого всех файлов, входящих в 
   состав приложения guidance.


guidance.vcproj
   Основной файл проекта VC++, автоматически создаваемый с помощью 
   мастера приложений.
   В файле представлены сведения о версии Visual C++, использованной 
   при создании файла, а также о параметрах платформы, конфигурации и 
   проекта, заданных с помощью мастера приложений.

guidance.cpp
   Основной исходный файл приложения.

/////////////////////////////////////////////////////////////////////////////
C помощью мастера приложений автоматически создаются следующие ресурсы:

guidance.rc
   Перечень всех ресурсов Microsoft Windows, используемых в программе. К ним 
   относятся значки, растровые изображения и курсоры, хранящиеся в подкаталоге 
   RES. Этот файл можно редактировать непосредственно в Microsoft Visual C++.

Resource.h
   Стандартный файл заголовков, в котором определяются новые идентификаторы 
   ресурсов.
   В Microsoft Visual C++ выполняется чтение и обновление содержимого этого 
   файла.

guidance.ico
   Файл значка для приложения (32x32).
   Этот значок включается в основной файл ресурсов guidance.rc.

small.ico
   Файл, содержащий уменьшенную версию значка приложения (16x16). Этот значок 
   включается в основной файл ресурсов guidance.rc.

/////////////////////////////////////////////////////////////////////////////
Другие стандартные файлы:

StdAfx.h, StdAfx.cpp
   Эти файлы используются для построения файла предкомпилируемых заголовков 
   (PCH) guidance.pch и файла предкомпилируемых типов 
   StdAfx.obj.

/////////////////////////////////////////////////////////////////////////////
Общие заметки:

С помощью комментариев «TODO:» в мастере приложений обозначаются фрагменты 
исходного кода, которые необходимо дополнить или изменить.

/////////////////////////////////////////////////////////////////////////////
